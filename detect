#!/bin/sh -l
set -eu
[ ! -f "$1" ] && echo "global.json does not exist at $1"
[ ! -f "$1" ] && exit 1
VERSION=$(sed -n 's|.*"version":\s*"\([^"]*\)".*|\1|p' "$1")

echo ".NET Version: $VERSION"
echo "DOTNET_VERSION=$VERSION" >> $GITHUB_ENV
echo "DOTNET_VERSION=$VERSION" >> $GITHUB_OUTPUT
